# nv-counter

Minimal demo of the neurova framework: a `.nv` single-file component
compiled by `@neurova/vite-plugin` and rendered with `@neurova/runtime`.

```bash
pnpm install
pnpm -F nv-counter-example dev
```
